# 用 AI 把示例改成你的城市

你不需要先学会写代码。把整个项目文件夹交给能读取、修改本地文件的 AI 编程工具，例如 Codex、WorkBuddy、ZCode、Hermes 或 OpenClaw，再按下面的顺序对话即可。

> 关键原则：AI 可以负责画风和代码，但城市的相对方位必须先查证、再固定到代码坐标中。不要让生图模型临时决定景点位置。

## 第一步：先让 AI 调研，不要急着改代码

复制下面这段，把方括号里的内容换成你的城市：

```text
我要把 Pixel City Walk 的济南示例改成【城市名】。

现在先不要修改代码，也不要生成图片。请先搜索当地政府、文旅部门、景区官网或可靠地图资料，整理 5～8 个代表性景点，并输出：
1. 景点名称和一句准确简介；
2. 景点之间的真实相对方位（北、南、东、西、东北等）；
3. 适合合并为同一漫游区域的景点；
4. 每条事实所依据的来源链接；
5. 一张纯文字方位草图，顶部为北。

如果资料互相冲突，请标出来让我确认。未经我确认，不要进入下一步。
```

你需要人工看一眼结果。至少确认“谁在北边、谁在南边、相邻景点是否真的相邻”。

## 第二步：让 AI 改造项目

确认方位表后，复制下面这段：

```text
请读取当前 Pixel City Walk 项目的 README.md、CITY_CUSTOMIZATION_GUIDE.md、index.html 和 assets 目录，把济南示例改成【城市名】版本。

要求：
1. 保留现有的进入流程、NPC 引导、人物移动、景点介绍、打卡、结束旅程和 PNG 下载功能；
2. 根据我确认的方位表，先为每个景点建立固定 x/y 坐标，顶部代表北；
3. 总览地图、局部场景、明信片中的景点位置必须使用同一套相对方位；
4. 替换城市名、景点名、NPC 文案、介绍文字、配色和图片资源；
5. 不得编造历史、地址、开放时间或票价；不确定的内容宁可删除；
6. 不要覆盖第三方素材许可文件，并更新 ASSET_LICENSES.md，写清新增图片的来源与许可；
7. 先在本地启动静态服务器，分别测试桌面与手机尺寸；
8. 测试昵称输入、移动、点击景点、打卡、结束旅程、生成及下载明信片；
9. 完成后列出修改文件、测试结果和仍需我人工确认的内容。

先给我一份改造清单和坐标表，等我确认后再修改文件。
```

## 第三步：生成城市总览地图

地图图片只负责提供视觉底图，实际可点击位置仍以代码中的坐标为准。可以把下面的提示词交给你常用的生图工具：

```text
生成一张【城市名】主题的横向像素游戏世界地图，俯视与轻微等距视角结合，16-bit 像素美术，明亮、细节丰富、适合作为网页小游戏总览底图。

画面顶部为正北。请严格遵循这份经过确认的相对方位表：
【粘贴景点方位表，例如：A 位于最北；B 在 A 东南；C 在城市中心偏西；D 在最南】。

把【5～8 个景点】抽象成可辨认的像素地标，并用道路、河流、绿地或街区自然连接。画面中不要生成文字、按钮、图标、定位针、边框、水印或 UI，景点名称将由网页代码覆盖。它是艺术化城市地图，不用于真实导航。
```

生成后先看方位，不要只看“好不好看”。如果位置不对，应重新生成或人工修改底图。

## 第四步：生成旅行票根风格参考图

下面的提示词用于确定视觉方向。项目运行时的最终明信片由浏览器 Canvas 绘制，不会为每位访客调用 AI，也不会产生生图 API 费用。

```text
围绕【城市名】生成旅行纪念票券视觉，画幅 16:9。整体呈横置实体票根形态：左侧约七成宽为城市插画地图，右侧约三成宽为洁白信息票根，中间以竖直虚线打孔撕线分隔；票身为圆角矩形，左缘保留半圆缺齿，轻柔浮于浅青与奶黄几何色块背景，并带柔和投影；右缘穿出细红绳系结，形成可触摸的实体纪念物感。

左侧面板以细稳手绘墨线、薄透水彩和轻像素质感表现城市。顶部必须代表北方，并严格遵循下列真实相对方位：
【粘贴已确认的方位表】。

以【核心景点】为视觉中心，其他景点按真实相对位置分层排布，用河流、道路、湖泊、山体或城市街区连接。可以艺术化比例，但不得交换南北、东西关系。加入少量人物、车辆或城市生活点景建立尺度。天空或水面使用淡蓝水彩晕染，整体线稿连贯、填色半透明、清透温暖，不做厚重油画。

右侧票根保持干净白底信息层：顶部小圆角纪念章；醒目的日期编号【MMDD】；城市名【城市名】；昵称【用户昵称】；日期【YYYY-MM-DD】；“宜：【今日宜】 / 忌：【今日忌】”；小指南针、英文字样与底部条码。文字如无法准确生成，可留出排版区域，交由网页代码绘制。

材质为奶油纸面和干净印刷感，无泛黄做旧、污渍或折痕。主色使用【城市主题色】、暖砖红、玻璃蓝、奶油白与淡天蓝。必须保持“左插画、右编号、撕线票根、软彩水彩、几何底色轻浮投影、红绳收口”的结构。禁止改成普通海报、纯摄影明信片或无票根结构的插画页。
```

## 济南示例坐标

济南示例采用“北湖、南山、中间泉城”的抽象结构：

```text
北 / 顶部：大明湖
大明湖南侧偏东：曲水亭街
中部偏西：趵突泉
趵突泉以东：泉城广场
泉城广场东南、沿护城河：黑虎泉·护城河
南 / 底部：千佛山
```

对应的明信片坐标位于 `index.html` 的 `drawTravelCard()` 函数中：

```js
const points = {
  daming:  { x: 690, y: 205 },
  oldcity: { x: 760, y: 350 },
  baotu:   { x: 300, y: 450 },
  square:  { x: 585, y: 480 },
  heihu:   { x: 860, y: 555 },
  qianfo:  { x: 575, y: 720 }
};
```

换城市时，先调整这张坐标表，再让 AI 绘制河流、道路、湖泊和山体。这样生成的明信片会忠于你的城市结构，而不是每次随机拼贴。

## 济南票根实际成图提示词

项目中的 `assets/jinan-travel-ticket-base.png` 使用下面这份提示词思路生成。你可以直接复制，再把济南和景点名称换成自己的城市：

```text
生成一张济南主题旅行纪念票券，16:9 横向画幅。整体呈横置实体票根：左侧约七成宽为手绘水彩城市地图，右侧约三成宽为洁白信息票根，中间以竖直虚线打孔撕线分隔。票身为圆角矩形，左缘带半圆缺齿，轻柔浮于浅青与奶黄几何色块背景上并带柔和投影，右缘穿出细红绳系结。

左侧必须是一张有明确北向的济南艺术地图，并严格保持真实相对方位：大明湖位于最北、画面顶部；曲水亭街位于大明湖南侧偏东；趵突泉位于中部偏西；泉城广场位于趵突泉以东；黑虎泉与护城河位于泉城广场东南侧；千佛山位于最南、画面底部。六个景点用水系、街巷和珊瑚红圆点旅行路线自然连接，不得交换南北或东西关系。

画面使用细稳手绘墨线与薄透水彩：大明湖呈开阔蓝色湖面与柳树；曲水亭街呈泉水老街；趵突泉表现三股泉水与亭子；泉城广场表现蓝色泉标；黑虎泉表现虎头吐水并连接护城河；千佛山表现南部青山、塔与寺庙。城市街区作为浅灰米色地图纹理铺底，整体清透、细腻、有旅行手账温度，不做厚重油画，不做像素路线示意图。

右侧票根保持干净奶油白纸面：顶部红色圆角印章写“泉城纪念”；超大黑色编号“0721”；标题“济南漫游”；副标题“我的泉城足迹”；日期“2026-07-21”；文案“宜：看泉  忌：赶路”；下方放小指南针、荷花与泉水图标、英文“JINAN TRAVEL MEMORY”和条码。字体呈干净印刷与手写旅行证件混合感。

材质锁定奶油纸面、干净印刷感，无泛黄做旧、无污渍折痕。色调以暖砖红、赤陶褐、泉水蓝、奶油白、淡天蓝和柳绿色为主。必须保持“左插画右编号、竖直撕线票根、软彩水彩、几何底色轻浮投影、右侧红绳收口”的结构。禁止改成普通海报、摄影明信片、信息图或无票根结构的插画页；禁止把景点随意拼贴到错误方位。
```

实际使用时，把生成并确认过的图片保存为 `assets/你的城市-travel-ticket-base.png`，再让 AI 修改 `index.html` 中的 `ticketImage.src`。网页下载时只读取这张固定成图，不需要每位访客重新调用生图模型。

## 发布前检查

- 景点南北、东西关系是否正确；
- 景点介绍是否有可靠来源，是否含过期票价或开放时间；
- 新图片是否为自己创作、AI 生成或拥有可再分发许可；
- `ASSET_LICENSES.md` 是否已更新；
- 页面是否注明非官方产品、艺术地图不用于导航；
- 手机横屏、竖屏和桌面端是否都能操作；
- 下载的 PNG 是否包含昵称、日期、路线和正确的景点位置。

如果 AI 一次改得不理想，不要让它推倒重做。直接指出一个问题，例如“黑虎泉应该在泉城广场东南侧”，让它只改坐标与对应视觉，逐项验收会更稳。
