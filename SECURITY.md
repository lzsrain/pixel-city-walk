# 安全政策

## 支持范围

当前维护范围为 GitHub 默认分支上的最新版本。历史副本和第三方部署由其维护者自行负责。

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。不要在公开 Issue 中发布可被直接利用的漏洞细节、个人信息或凭证。

报告中请包含：

- 受影响的文件或功能；
- 可重复的最小步骤；
- 可能的影响；
- 建议修复方式（如有）。

本项目是纯静态网页，不需要服务器密钥。若某个第三方部署自行加入统计、登录、数据库或外部 API，这些扩展不属于本仓库默认安全边界。
