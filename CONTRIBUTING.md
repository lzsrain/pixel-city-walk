# 贡献指南

感谢你愿意改进济南像素漫游。

## 开始之前

1. 搜索现有 Issue，避免重复提交；
2. 功能建议应说明用户场景、预期行为和验收方式；
3. 涉及城市地图、人物、字体、音效或图片时，必须同时提供可验证的来源和许可证；
4. 不要提交来源不明、仅限个人使用或禁止再分发的素材。

## 本地运行

```bash
python -m http.server 8080
```

访问 `http://127.0.0.1:8080/`。项目没有构建步骤。

## 提交要求

- 保持项目无需后端即可运行；
- 不提交 API Key、密码、Token、个人信息或真实定位数据；
- 桌面端改动应同时检查移动端横屏；
- 新增景点应在对应城市的 `city.json` 中添加名称、介绍来源、触发位置和基本验证步骤；
- 新增素材应更新 `ASSET_LICENSES.md`；
- 不得静默更换已有文件的许可证。

## Pull Request

Pull Request 应包含：

- 改动解决的问题；
- 手工验证步骤；
- UI 改动前后截图；
- 新增第三方内容的来源与许可证；
- 是否影响浏览器本地存档。

提交贡献即表示你有权提供相关内容，并同意原创代码按 MIT、明确标注的原创美术按对应素材许可证分发。
