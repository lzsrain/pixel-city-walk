# Pixel City Walk 更新记录

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的基本格式。

## [Unreleased]

## [1.2.0] - 2026-07-27

### 新增

- 明信片个性化：票面按 `city.json` 的 `postcard.overlay` 配置绘制玩家昵称、日期、漫游路线、区域/风景统计和项目网址，模板含 `{name}` 等占位符，引擎保持城市无关；
- 本地进度保存：昵称、路线、手账、步数存入 localStorage（按城市分键），刷新或再次访问自动恢复并跳过引导；顶栏新增"重新开始"按钮清除进度。

### 修复

- 票根底图去除写死的“0721”“2026-07-21”“我的泉城足迹”和“宜/忌”文字（原区域按纸张纹理修复留白）；编号、昵称副标题、日期、宜忌改为引擎按生成当天日期动态绘制，新增 `{mmdd}` 占位符与每行 `letterSpacing` 配置，整张票面保证同一天，样式坐标全部在 `city.json` 的 `postcard.overlay` 中；
- 修复 NPC 向导精灵图不显示的问题：CSS 变量中的相对路径改为运行时解析成绝对 URL，`style.css` 回退路径同步修正（原路径相对样式表解析导致 404）；
- 修复手机端"结束漫游"页面明信片被强制拉高导致的大面积空白，竖屏/横屏均按 16:9 自适应；
- 欢迎语标题中的换行符现在正确换行显示；
- 修正 README 示例图路径与指南中的旧票根路径；正文中的英文双连字符改为中文破折号。

## [1.1.0] - 2026-07-24

### 架构

- 将单文件 `index.html` 拆分为 HTML 骨架、`css/style.css` 和 `js/engine.js`；
- 城市内容（区域、景点、坐标、NPC、配色、文案）抽离为 `cities/jinan/city.json` 数据包；
- 引擎代码不再包含任何城市专属数据，换城市只需新建 `cities/<城市名>/` 目录并修改 `city.json`；
- 支持通过 URL 参数 `?city=城市名` 加载不同城市；
- 直接双击 `index.html` 打开时，会显示"请用静态服务器打开"的友好提示而非白屏。

### 体验

- 扩充全部 13 个景点的介绍文案，主文从 20-29 字加长到 58-85 字，事实标签更具体；
- 修复景点介绍弹窗的背景图片定位，改为以景点坐标为中心放大显示局部地图；
- 新增水域碰撞检测，玩家不能走进大明湖、趵突泉池和护城河，沿水边可滑动通过。

### 新增

- 16:9 横向旅行票根明信片，按固定坐标呈现济南景点和玩家漫游路线；
- 任意城市 AI 改造教程及可复制提示词；
- 城市方位核对、素材许可和发布前检查清单。

### 调整

- 将原竖版旅行名片升级为可下载的像素明信片。

## [1.0.0] - 2026-07-20

### 新增

- 济南像素世界总览和自由漫游；
- 桌面键盘、鼠标点击与移动端触控操作；
- 景点介绍、旅行手账与本地进度；
- 玩家昵称、NPC 引导和旅行名片 PNG；
- MIT 代码许可证、素材许可证和开源社区文件。

### 示例城市

- 首个内置示例为“泉城漫游记（济南）”。
